# 作業ログ (history.md)

## 2026-04-25 12:26
- プロジェクト開始。
- 「Volume2」クローンアプリの開発計画を策定。
- 技術スタックとして Electron + React + shadcn/ui を選定。
- `implementation_plan.md` を作成。
- Vite + React プロジェクトの初期化。
- Electron (main.cjs, preload.cjs) の実装。
- PowerShell を利用した音量制御ロジックの実装。
- Tailwind CSS / Glassmorphism を採用したプレミアムなUIの実装。
- ポート衝突による起動エラーを修正（Viteのポートを5173に固定し、既存プロセスをクリーンアップ）。
- Tailwind CSS v4 の PostCSS 互換性エラーを修正（@tailwindcss/postcss の導入と設定更新）。
- OSD（オン・スクリーン・ディスプレイ）モードの実装。
- グローバルホットキー（Ctrl+Up/Down）による音量調整に対応。
- システムトレイアイコンの追加と、音量変更時のみUIが表示される動作を実現。
- ボリューム制御ロジックを信頼性の高い `loudness` ライブラリに移行（PowerShellのCOMエラーを回避）。
- OSDの表示位置を画面解像度に合わせて動的に調整（右下固定）するように改善。
- OSDのチラつき（再表示）を抑制し、操作中は表示を維持、操作終了から2秒後に消えるように挙動を修正。
- UIアニメーションを整理し、音量変化時のスムーズな遷移を実現。
- ミュート機能（Ctrl+M）の実装と、OSDでの視覚的フィードバック（赤色表示・アイコン切り替え）を追加。
- 音量調整時に自動的にミュートを解除するロジックをメインプロセスに実装。
- マウスホイール（タスクバー上）による音量調整と、ホイール押し込みによるミュート切り替えを実装（uIOhookのタイポを修正）。
- `walkthrough.md` を更新。
- 設定の永続化（electron-store）を導入。
- 音量調整の変化幅（ステップ数）を 1%〜10% の間で設定できる機能を追加。
- `electron-store` のロードエラーを回避するため、独自の簡易保存ロジック（SimpleStore）に移行。
- OSDに設定ボタンを追加し、UI上でステップ数を変更可能に。
- 設定画面表示時のウィンドウサイズ自動調整と、表示位置の補正（上方向に広がるように修正）を実装。
- 設定画面表示中はOSDが自動で消えないように挙動を修正。
- 設定画面を閉じた際に即座にウィンドウを非表示にするように改善。
- キー操作による音量調整を廃止し、マウス操作（タスクバー上）に限定。
- アプリ名を「Volume App」に統一（UI、トレイ、ドキュメント）。
- `electron-builder` を導入し、実行ファイル（EXE）のビルド設定を追加。
- ポータブル版（インストール不要）のビルド構成を作成。

## 2026-04-26 21:36
- 設定ボタンの横に「使い方」ボタン（HelpCircle）を追加。
- アプリの操作方法を表示するヘルプ画面を実装。
- 設定画面とヘルプ画面の表示状態管理を整理。

## 2026-04-26 21:46
- OSDにホバーしている間は自動で閉じないように機能を改善。
- マウスがOSDから離れた際に2秒後に閉じるように挙動を調整。
