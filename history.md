# 作業ログ (history.md)

## 2026-04-25 12:26
- プロジェクト開始。
- 「Volume2」クローンアプリの開発計画を策定。
- 技術スタックとして Electron + React + shadcn/ui を選定。
- `implementation_plan.md` を作成。
- Vite + React プロジェクトの初期化。
- Electron (main.cjs, preload.cjs) の実装。
- PowerShell を利用した音量制御ロジックの実装。
- Tailwind CSS / Glassmorphism を採用したプレミアムなUIの実装。
- ポート衝突による起動エラーを修正（Viteのポートを5173に固定し、既存プロセスをクリーンアップ）。
- Tailwind CSS v4 の PostCSS 互換性エラーを修正（@tailwindcss/postcss の導入と設定更新）。
- OSD（オン・スクリーン・ディスプレイ）モードの実装。
- グローバルホットキー（Ctrl+Up/Down）による音量調整に対応。
- システムトレイアイコンの追加と、音量変更時のみUIが表示される動作を実現。
- ボリューム制御ロジックを信頼性の高い `loudness` ライブラリに移行（PowerShellのCOMエラーを回避）。
- OSDの表示位置を画面解像度に合わせて動的に調整（右下固定）するように改善。
- OSDのチラつき（再表示）を抑制し、操作中は表示を維持、操作終了から2秒後に消えるように挙動を修正。
- UIアニメーションを整理し、音量変化時のスムーズな遷移を実現。
- ミュート機能（Ctrl+M）の実装と、OSDでの視覚的フィードバック（赤色表示・アイコン切り替え）を追加。
- 音量調整時に自動的にミュートを解除するロジックをメインプロセスに実装。
- マウスホイール（タスクバー上）による音量調整と、ホイール押し込みによるミュート切り替えを実装（uIOhookのタイポを修正）。
- `walkthrough.md` を更新。
- 設定の永続化（electron-store）を導入。
- 音量調整の変化幅（ステップ数）を 1%〜10% の間で設定できる機能を追加。
- `electron-store` のロードエラーを回避するため、独自の簡易保存ロジック（SimpleStore）に移行。
- OSDに設定ボタンを追加し、UI上でステップ数を変更可能に。
- 設定画面表示時のウィンドウサイズ自動調整と、表示位置の補正（上方向に広がるように修正）を実装。
- 設定画面表示中はOSDが自動で消えないように挙動を修正。
- 設定画面を閉じた際に即座にウィンドウを非表示にするように改善。
- キー操作による音量調整を廃止し、マウス操作（タスクバー上）に限定。
- アプリ名を「Volume App」に統一（UI、トレイ、ドキュメント）。
- `electron-builder` を導入し、実行ファイル（EXE）のビルド設定を追加。
- ポータブル版（インストール不要）のビルド構成を作成。

## 2026-04-26 21:36
- 設定ボタンの横に「使い方」ボタン（HelpCircle）を追加。
- アプリの操作方法を表示するヘルプ画面を実装。
- 設定画面とヘルプ画面の表示状態管理を整理。

## 2026-04-26 21:46
- OSDにホバーしている間は自動で閉じないように機能を改善。
- マウスがOSDから離れた際に2秒後に閉じるように挙動を調整。

## 2026-04-26 21:52
- 設定ボタンとヘルプボタンの位置を少し上に調整。

## 2026-04-26 21:57
- 設定ボタンとヘルプボタンを常時表示するように変更。
- 音量テキストと数字の間隔を調整（justify-between から gap-3 に変更）。

## 2026-04-30 13:23
- 実行ファイル（EXE）の再ビルドを実施。
## 2026-05-09 18:26
- マルチモニター環境において、サブモニター上のタスクバー以外の場所で音量調整が反応してしまう問題を修正。
- `isMouseOverTaskbar` 関数を刷新し、マウスがある各モニターのワークエリア（タスクバーを除いた領域）を基準に判定するように改善。
- 音量調整時に表示される OSD が、現在操作しているモニターの右下に動的に表示されるように機能を強化。
- 設定画面を開く際も、現在のマウス位置に基づいて適切なモニターに表示されるように修正。

## 2026-05-09 18:35
- 高DPIモニターでの初回音量調整時にOSDウィンドウの一部が欠ける問題を修正。
- `showInactive()` 後に再度 `setBounds()` を呼び出すことで、描画領域を確定させる処理を追加。

## 2026-05-14 10:37
- ボリュームの桁数（1桁〜3桁）の変化によってOSDのレイアウトやウィンドウ幅が動かないように修正。
- `App.jsx` のボリューム表示部分に固定幅（`w-16`）と右寄せ（`text-right`）を設定。
- OSDコンテナ自体の幅を `w-[300px]` に固定。
- Electronの `BrowserWindow` 設定に `resizable: false` と `maximizable: false` を追加し、ウィンドウサイズの固定を徹底。

## 2026-05-14 10:43
- 設定画面（またはヘルプ画面）からフォーカスが外れた際に、自動的にウィンドウを閉じるように変更。
- `main.cjs` に `blur` イベントリスナーを追加し、設定モード中は非表示にする処理を実装。
- `preload.cjs` と `App.jsx` を更新し、ウィンドウが閉じた際にReact側の表示状態もOSDモードにリセットされるように連携。

## 2026-05-14 10:50
- 変更を反映した実行ファイル（EXE）の再ビルドを実施。

## 2026-05-17 21:46
- 音量調整またはミュート切り替え時に、時々UI（OSDウィンドウ）が表示されなくなる不具合を修正。
  - 原因：非表示タイマー発火とホイールスクロールイベントの競合（`isVisible()` の誤検知）、Chromiumの透明ウィンドウ特有の再描画バグ、設定モード（`isSettingsMode`）の状態不整合。
  - 対策：
    - `win.isVisible()` の判定を排し、OSD呼び出し時は常に `win.showInactive()` を呼び出して描画を強制。
    - 表示の都度 `win.setAlwaysOnTop(true, 'screen-saver')` を再適用し、他ウィンドウやタスクバーの背後に隠れるのを防止。
    - `win.webContents.invalidate()` を追加し、透明ウィンドウが再表示された際に描画が更新されないChromiumのバグを回避。
    - 設定モード中にタスクバーでスクロール操作された場合、自動的に設定画面を閉じて OSD モードに移行する自己修復ロジックを `showOSD()` 内に実装。これにより状態の不整合を完全に排除し、直感的なUXを提供。

## 2026-05-29 09:25
- 上書きビルド用コマンドの問い合わせに対応。
  - `dist` および `release` ディレクトリを事前にクリアした上で `npm run dist` を実行するコマンドを提示。

## 2026-05-29 09:32
- Windowsアップデート後にアプリが起動直後にクラッシュする不具合への暫定対策を実装。
  - 原因：Windowsアップデートに伴うGPUドライバやExploit Protection等のセキュリティ設定とChromiumのサンドボックス/GPUアクセラレーションの競合。
  - 対策：`src-electron/main.cjs` の初期化プロセスに、`app.commandLine.appendSwitch('disable-gpu')` および `app.commandLine.appendSwitch('no-sandbox')` を追加。これにより、起動オプションを手動で付与しなくてもアプリが安全に起動できるように改善。

## 2026-05-29 09:47
- 上書きビルド用コマンドの問い合わせに対応。
  - フロントエンドのビルドとElectronのビルドを連続して行うための上書きビルドコマンド（`npm run build:frontend && npx electron-builder --win`）を案内。
